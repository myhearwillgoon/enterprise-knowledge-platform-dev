# EKP runtime state directory - never committed
.ekp/

# Project persistent memory - operator-local, points at internal assets.
# Only the .example.md template is public; the filled-in copy stays local.
memory/PROJECT_CONTEXT.md

# Codex session caches and temp prompt files
*.codex-tmp
combined-prompt.txt

# OS / editor noise
.DS_Store
*.swp
*.swo
*~
.vscode/
.idea/

# Node tooling (if anyone runs lint/format inside the skill repo)
node_modules/
package-lock.json
yarn.lock
